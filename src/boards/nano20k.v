
`define CONFIG_V

`define PLL_R
`define SDRAM_3CH
`define CHIP_DSPn
// `define CHIP_GSU

`define CONTROLLER_DS2
`define CONTROLLER_SNES

// `define DISABLE_SNES
// `define DISABLE_SMP

package configPackage;  

localparam SDRAM_DATA_WIDTH = 32;
localparam SDRAM_ROW_WIDTH = 11;

endpackage

