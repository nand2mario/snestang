
`define NANO
`define SDRAM_3CH
// `define CHIP_DSPn
// `define CHIP_GSU

// `define DISABLE_SNES
// `define DISABLE_SMP

package configPackage;  

localparam SDRAM_DATA_WIDTH = 32;
localparam SDRAM_ROW_WIDTH = 11;

endpackage

